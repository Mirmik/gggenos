




#hfiles = Dir[$relpath + '**/*']

#files = []
#hfiles.each {|f| files << f[$relpath.size..-1] if f.index(/\/*HIDE\/*/)==nil}

#files.each { |f| 
#if f.index(/.cpp$/) or f.index(/.c$/) then $source = $source + [f] end
#}

#$pack = [["genos_libc_string",{}]]

#$name = "genos_libc"
#$target = "libc.a"
#$incl = ["%s%s" % [$relpath, "include"]]
#declare_module