

var _cc_source = scantree_regex(__dirpath__(), ".c$", "HIDE");
var cc_source = sublast_list(unvar(_cc_source), length(__dirpath__()) + 1);

module genos_libc {
	source = cc_source;
	options = ["GENOS"];
	include = ["include"]
};